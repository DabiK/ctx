# Issue tracker

Use GitHub Issues for ctx work. The intended repository is the GitHub repository created from this working directory. Publish build specifications as GitHub issues and create implementation work as one GitHub issue per tracer-bullet ticket.

Each implementation issue must state its blocking GitHub issues in a **Blocked by** section. Publish blockers before the tickets that reference them.

